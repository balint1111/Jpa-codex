# Repository Guidelines

This repository contains a full stack project skeleton.

- Follow instructions in `backend/AGENTS.md` and `frontend/AGENTS.md` when modifying files in those directories.
- Run tests (`gradle test` for backend, `npm test` for frontend) before committing changes that affect code.

