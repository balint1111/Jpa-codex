# Frontend Agent Instructions

This directory contains an Angular project skeleton.

## Install Dependencies
Run `npm install` in this directory.

## Development Server
Use `npm start` to run the dev server on `http://localhost:4200`.

## Tests
Run `npm test` to execute unit tests with Karma.

